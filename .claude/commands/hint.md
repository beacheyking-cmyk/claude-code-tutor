# /hint

Give the student a nudge WITHOUT solving the problem.

- One hint at a time
- Point to the right concept, not the answer
- Ask a leading question if possible
- Track hints used — no XP bonus if hints were used this lesson
- After 3rd hint on same problem, suggest /solution instead
